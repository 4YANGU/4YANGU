// Retired storefront engine stub. The JSON-driven renderer was removed per the
// storefront re-architecture — live storefronts are server-stamped AI skins.
// (StorefrontRenderer.tsx stays as the internal fallback for legacy stores.)
// Never imported anywhere; kept as a stub so file-deletion steps are unnecessary.
export default function JsonStorefront() { return null; }
