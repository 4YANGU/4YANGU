============================================================
  STOYANGU — READ THIS FIRST (fixes the Vercel 12-function error)
============================================================

WHY VERCEL KEEPS REFUSING YOUR DEPLOYS
--------------------------------------
Vercel turns EVERY .js file inside the api/ folder into one
Serverless Function. The Hobby plan allows a maximum of 12.

Right now your repo's api/ folder has 16 files, because 4 extra
files were added by an earlier update pack and never removed:

    api/batch.js        <-- must not exist
    api/engage.js       <-- must not exist
    api/db-client.js    <-- must not exist
    api/db-wake.js      <-- must not exist

A zip cannot delete files from your repo. That is why applying
zip after zip kept failing: the 4 extra files stayed put.

THE FIX — CHOOSE ONE OF THESE (A is automatic)
----------------------------------------------

A) DO NOTHING EXTRA — let the automatic cleaner do it.
   This pack contains .github/workflows/api-cleanup.yml.
   Once that file is in your repo, every push automatically
   deletes the 4 extra files and Vercel deploys the clean state
   a moment later. If the cleaner itself fails, open your repo on
   GitHub -> Settings -> Actions -> General -> Workflow permissions
   -> choose "Read and write permissions" -> Save, then push again.

B) DELETE THEM BY HAND (takes 1 minute, works everywhere).
   On GitHub, open each of these 4 files and tap Delete:
       api/batch.js
       api/engage.js
       api/db-client.js
       api/db-wake.js
   Commit each deletion. Deploy. Done.

C) IF AN AI TOOL / APP BUILDER EDITS YOUR CODE:
   Tell it exactly this:
   "Delete the files api/batch.js, api/engage.js, api/db-client.js
   and api/db-wake.js from the project, and never recreate them.
   The api folder must contain exactly 12 files."

HOW YOU KNOW IT IS FIXED
------------------------
The api/ folder must contain EXACTLY these 12 files:

   applications.js   cron.js          media.js       orders.js
   products.js       seo.js           storefront.js  stores.js
   subscriptions.js  track.js         dashboard.js   notifications.js

12 files = 12 functions = Vercel Hobby plan deploys with no issues.

EVERYTHING ELSE IN THIS PACK
----------------------------
- The original 12 api routes, byte-for-byte (the arrangement your
  site has always deployed with).
- vercel.json with routes/headers/schedules only and NO env block
  (your Supabase keys stay in the Vercel dashboard, as always).
- The install fix: restored app icons (public/favicon-192.png,
  public/icon-maskable-512.png), bumped service-worker cache, and
  persistent installed-state — so StoYangu fully installs into the
  phone's app list and the "Install app" button never reappears
  after a refresh on the manage-my-store page.
- Demo founder login: tap "Open founder demo" — no password.
- Owner test login: WhatsApp 0793 825 499 / password BestonDemo123!
